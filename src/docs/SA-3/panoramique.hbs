<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{pageTitle}}</title>
    <style>
      /* For screen display */
      body {
        font-family: Arial, sans-serif;
        padding: 15px;
        display: flex;
        flex-direction: column;
        align-items: center;
        overflow-y: scroll;
        margin: 15px;
      }

      .container {
        border: 2px solid black;
        width: 280mm; /* A4 width in landscape */
        height: 170mm; /* A4 height in landscape */
        margin: 10mm; /* Margin for print */
        box-sizing: border-box;
        padding-bottom: 10px;
      }

      .title {
        text-align: center;
        font-size: 24px;
        font-weight: bold;
        padding: 10px;
        background-color: #e0e0e0;
        border-bottom: 2px solid black; /* Increased border thickness */
      }

      .content {
        background-color: white;
        padding: 10px;
        height: calc(100% - 40px); /* Adjust for title height and padding */
        box-sizing: border-box;
      }

      .square {
        width: 100%;
        height: 100%; /* Adjust height as needed */
        background-color: white;
        display: flex;
        justify-content: center;
        align-items: center;
        position: relative;
      }

      .square img {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
      }
    </style>
  </head>
  <body>
    <div class="container" style="position: relative">
      <img
        src="{{logoSrc}}"
        alt="{{logoAlt}}"
        style="
          position: absolute;
          width: 180px;
          z-index: 3;
          left: -8px;
          top: -8px;
        "
      />
      <div class="title">{{panoramaTitle}}</div>
      <div class="content">
        <div class="square">
          <img src="{{panoramaImageSrc}}" alt="{{panoramaAltText}}" />
        </div>
      </div>
    </div>
  </body>
</html>
